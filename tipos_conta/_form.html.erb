<%= form_for(tipo_conta) do |f| %>
  <% if tipo_conta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_conta.errors.count, "error") %> prohibited this tipo_conta from being saved:</h2>

      <ul>
      <% tipo_conta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %>
    <%= f.text_field :codigo %>
  </div>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
